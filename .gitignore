# Node / 前端构建
node_modules/
dist/

# Rust / Tauri 构建产物
target/
gen/

# 系统与日志
*.log
.DS_Store
Thumbs.db
desktop.ini

# 签名密钥（泄露即可冒充应用发布，切勿提交）
keystore/

# 小程序代码上传密钥（miniprogram-ci 用，泄露可冒充提交代码）
private.key
*.upload.key
upload-keys/

# Isolated desktop verification build
/le-time-management/src-tauri/target-care-windows/
/releases/

# Upstream native compilation caches
/vendor/shiguangschedule/**/build/
/vendor/shiguangschedule/.gradle/
/vendor/shiguangschedule/.kotlin/
/vendor/shiguangschedule/local.properties
/le-time-management/src-tauri/native/shiguang/

# WorkBuddy AI workspace
.workbuddy-ai/

# 覆盖前的本地备份快照（含改动前的源码）
/.backup/

# Downloaded archives
*.zip

# Old APK in root
/Le时间管理_*.apk

# 开发期抓包与调试产物（含 SSO 会话 cookie / CASTGC JWT / 个人信息，泄露可冒充登录，切勿提交）
/le-time-management/dev/jw-*
# 教务门户 SPA 抓包临时目录（_nuxt_*.js / index.html 等，排查用，不入库）
/.tmp-*/

# Tesseract OCR 语言包（约 15 MB，本地识别用，不入库）
/eng.traineddata

# 本地工具缓存与 QA 产物(不入库)
/.workbuddy/
/.playwright-cli/
/output/
# ⚠️ 上一条 /output/ 是锚定仓库根的，**子目录下的 output/ 不匹配** ——
#    手机截图 / 探针产物都落在 le-time-management/output/，得单独写一条。
/le-time-management/output/
# 与本项目无关的目录（另有归属，只是放在同一工作区里）
/创客比赛/
cloudflare-pages/plugin-guide/.wrangler/

# 录屏用 HTML 动画（只入库页面与脚本，不入库截图与立绘素材）
# 立绘可由 `cp -r ~/.agents/skills/video-shot-demos/assets/examples/<项目>/{anan,shery}* <页面目录>/` 复原，
# _qc/*.png 由 `node ~/.agents/skills/video-shot-demos/scripts/shot.js` 重跑生成。
/动画演示/**/_qc/
/动画演示/**/anan - emotion rename/
/动画演示/**/shery - emotion rename/
/动画演示/**/*.png
/动画演示/**/*.jpg
