# v0.36.6 · 插件中心搜索工具行下移

发布版本：v0.36.6（三端同源，无插件变更）

## 界面

- **「⌕ 搜索开关 + 显示 N / M」下移到筛选按钮下面**（用户反馈）：顶部只留筛选档，
  不再多占一行；搜索展开时输入框仍出现在该行之下、卡片列表之上。
- 移动端（≤700px）搜索收起时给工具行补 12px 下边距，避免与卡片贴死。

## 验证

- 22/22 测试通过。
- 无头浏览器（Android UA + 390×844）实测：顺序为 筛选档 → ⌕+计数 → 卡片列表。
