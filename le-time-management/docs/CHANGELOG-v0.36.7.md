# v0.36.7 · 手机底栏缩矮

发布版本：v0.36.7（三端同源，无插件变更）

## 界面

- **底部导航栏缩矮**（用户反馈）：整体高度 67px → 53px。
  按钮最小高 54→46px（仍 ≥44px 触控目标）、内边距 6→3px、图标 26→22px、
  图标与文字间距 3→2px；容器上下 padding 4/6→2/4px；左侧设置齿轮同步收矮。
  `.view` 底部预留 74→62px。

## 修复

- 移动块的 22px/46px 原本被文件更后面的全局 `.nav button .ic{38px}` /
  `.nav button{min-height:50px}` 同优先级覆盖（实测 icW=38px、minH=50px），
  移动块选择器提为 `.rail .nav button` 后真正生效。

## 验证

- 22/22 测试通过；无头浏览器（Android UA + 390×844）实测
  railH=53 / btnH=46 / icW=22。
