{
  "name": "le-time-management",
  "private": true,
  "version": "0.79.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "node scripts/run-tests.mjs",
    "build": "node ../tools/sync-version.js --check && vite build",
    "check": "npm run test && npm run build",
    "preview": "vite preview",
    "tauri": "tauri",
    "check:feeds": "node scripts/check-feed-urls.mjs",
    "check:update-release": "node scripts/check-update-release.mjs",
    "check:update-release:full": "node scripts/check-update-release.mjs --download",
    "version:check": "node ../tools/sync-version.js --check"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.1",
    "@tauri-apps/plugin-global-shortcut": "^2.0.0",
    "jsqr": "^1.4.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0",
    "vite": "^6.0.3"
  }
}
