[package]
name = "letime"
version = "0.39.0"
description = "Le时间管理 · 时间块与四象限"
authors = ["yile"]
edition = "2021"

[lib]
name = "letime_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls", "charset", "gzip", "cookies"] }
encoding_rs = "0.8"
des = "0.8"
ecb = "0.1"
hex = "0.4"
base64 = "0.22"
ring = "0.17.14"
tiny_http = "0.12"
qrcode = { version = "0.14", features = ["svg"] }
zip = { version = "2", default-features = false, features = ["deflate"] }

[target."cfg(any(target_os = \"windows\", target_os = \"linux\", target_os = \"macos\"))".dependencies]
tauri-plugin-single-instance = "2"
tauri-plugin-global-shortcut = "2"

# 教务导入窗口的 Android 关闭通道：JniHandle::exec 回调里调 Activity.finish()
# （版本与 tauri 锁定的 jni 0.21 统一，避免双版本共存的类型不一致）
[target."cfg(target_os = \"android\")".dependencies]
jni = "0.21"

[profile.release]
codegen-units = 1
lto = true
strip = true
