# Le 时间管理 v0.24.1

## 改了什么

- 课程表教务导入新增 `.xlsx` / `.xls` 文件支持，可直接读取教务系统导出的 Excel 课表。
- 补充 `课程名`、`任课老师`、`教学场地`、`周星期`、`上课周数`、`起止节次` 等常见表头。
- 支持 `1-16周(单)`、`2-18周（双）` 等括号单双周写法。
- 支持英文星期值，如 `Monday` / `Mon`。
- 增加真实 Excel 二进制到课程数据的回归测试。

## 影响范围

- Windows、Android 课程表插件。

## 数据迁移

- 无数据结构变化。
