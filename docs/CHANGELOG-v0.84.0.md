# v0.84.0

## 课程表快捷导入

- 课程表右上角「⋯」菜单的“操作”区直接显示“导入教务”，无需先进入“我的”。
- 新入口复用原有教务导入页面、学校选择、登录导入、文件导入及返回栈，不新增重复流程。
- “导入教务”排在操作区首位，并使用与现有菜单一致的行高、图标和主题配色。

## 影响范围与兼容性

- 课程表插件版本升至 `3.9.0`，桌面端与 Android 端共用该入口。
- 不改变课表、课程或教务导入数据结构，无数据迁移。
- 微信小程序仅同步应用与插件目录版本信息，课程表插件仍标记为不可用。
