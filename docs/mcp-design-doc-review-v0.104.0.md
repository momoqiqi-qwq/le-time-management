# 设计文档与功能介绍页复核（v0.104.0）

> 日期：2026-09-25 · 范围：`docs/设计原则与整体架构说明.md`、`docs/features.html` 及 `README.md` 入口
> 背景：设计文档按 v0.103.0 源码的更新、功能介绍页入口已由另一会话写好（其后又补了 v0.104.0 一行）。
> 本次不重写，逐条对照源码复核，只修确有出入的地方。

## 一、复核结果

| 项目 | 做法 | 结论 |
|---|---|---|
| 「文件:行号」引用 | 93 处带行号引用（涉及 46 个文件）：文件存在、行号不越界、正文点到的标识符出现在引用行附近；自动比对未命中的 38 处逐条人工核对 | 93 / 93 正确 |
| 摘要规模 | 主机上按 v0.104.0 重算 | 前端 71 个文件 / 17,068 行、Rust 8 / 3,716、小程序 40 / 9,959、command 41、`store.js` 与 `ui.js` 各被 28 个模块引用、`pluginHost.js` 9 个、测试 80、内置插件 17、运行时依赖 4，全部一致 |
| 具体数字与说法 | 对照源码 | 权限位 14；`tide.util` 11 项 + `util.web.*` 14 项；提示音 8 种；AI 自动任务单轮 5 条、失败退避 10 分钟；插件 AI 对话 ≤24 条 / ≤60000 字符；自更新 SHA-256；Android 精确闹钟 14 天 / 40 条；桌面端未接入系统通知（通知插件在 `lib.rs:1994` 的 Android 条件编译块内，网页 `Notification` 分支只在浏览器调试时可达）；插件颜色分组一段；学习通五档筛选（`chaoxing-notify/main.js:808-826`、`:1081`），全部一致 |
| 格式与链接 | 换行、BOM、行尾空格、表格列数、相对链接 | 与 HEAD 一致；21 个相对链接中仅 README 两张概念图缺失（历史遗留，见第三节） |
| 功能介绍页 | Windows Chrome 实机自检 | 外部请求 0、控制台 0、异常 0，桌面 / 手机横向溢出 0，5 个文档页互链可达 |

## 二、本次修正（2 个文件、5 行）

- 设计文档头部「基准版本」v0.103.0 → v0.104.0，与文末最新修订行、代码版本一致；v0.104.0 修订行补记这一项。
- `features.html` 页头、页脚的版本标注 v0.103.0 → v0.104.0（页面已写入 v0.104.0 的学习通第五档）。
- `features.html` 学习通卡片注明五档筛选限桌面端与 Android 端：小程序原生版（`miniprogram/core/chaoxingCore.js`）没有这组筛选，而卡片标了「小程序」。
- 另：`output/` 下的图片内嵌版（`node tools/embed-doc-images.js docs/设计原则与整体架构说明.md`）原为 09-19 的 v0.70.0 旧稿，已按当前文档重新生成（不进仓库）。

## 三、未改动，需知悉

1. `docs/CHANGELOG-v0.104.0.md`「影响范围」写"小程序端该插件标记为 `unavailable`"，与事实不符：`miniprogram/core/pluginCatalog.js` 里 `chaoxing-notify` 是 `miniprogram: "native"`（标 `unavailable` 的是 school-notice、cppu-notify、ai-chat、github-readme）。准确说法是"小程序端为独立的原生实现，本版未改动，不含第五档"。该文件属 v0.104.0 的进行中产出，未代改。
2. `README.md` 第 53 行的 `ui-概念稿/renders/03.png`、`04.png` 自首次提交（1b5a722）起就不存在。
3. `timeParser.js:160` 的 `guessQuad()` 用 `new Date("YYYY-MM-DD")` 按 UTC 解析，东八区下"后天"截止会落入象限 II，比文档"两天内归象限 I"的口径少一天；属代码问题，文档写的是设计意图。
4. `cloudflare-pages/plugin-guide` 是独立部署的插件指南站（最后同步于 v0.18.0），不含功能介绍页；要公开发布需另行部署。
