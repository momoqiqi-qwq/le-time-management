# v0.75.0

## 改了什么

- 将时间块派生索引从“任意变更后全量清空”优化为按日期精准失效。
- 更新单个日期的时间块时，其他日期继续复用已排序的查询结果，减少周视图、多日统计和自动排程中的重复全表扫描。
- 补充缓存扫描次数回归测试，覆盖受影响日期重算、未受影响日期复用，以及整体数据替换后的全量失效。

## 影响范围

- Windows 桌面端与 Android 端共用的前端状态层。
- 小程序仅同步版本号，不涉及运行逻辑变化。

## 数据迁移

- 无。状态结构与已有数据格式均未改变。
