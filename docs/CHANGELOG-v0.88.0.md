# v0.88.0

## 新内容标签

- 新添加到任务表、收件箱和时间线的内容，在标题末尾显示红底白字的 `NEW` 胶囊标签。
- 新任务和新时间块在创建时写入新内容状态；从 CSV、Excel 或 ICS 合并导入的记录同样会被标记。
- 打开任务详情后清除该任务的 `NEW` 标签；展开时间线节点后清除对应任务或时间块的标签。
- 收件箱的新事项在处理完成前保留标签。
- 旧数据没有新内容状态时保持原样，不进行批量回填。

## 影响范围

- Windows 与 Android 的任务表、收件箱和时间线视图。
- 新增字段向后兼容，无需数据迁移。
