# v0.93.1

## 修复

- 修复应用内网页窗口打开学习通作业等页面后，关闭最新弹出的网页窗口会把前一个网页窗口一起关闭的问题。
- 应用内网页现在会把 `target=_blank` 与 `window.open` 注入到所有 frame 中处理，并拒绝原生关联子窗口，避免 WebView2 窗口生命周期互相牵连。

## 影响范围

- 桌面端：应用内网页窗口。
- Android / 小程序：无数据迁移。
