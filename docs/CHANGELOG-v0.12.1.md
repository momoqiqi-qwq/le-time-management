# v0.12.1

## 改动内容
- 设置入口改为右上角快捷菜单唤起的全屏弹窗，不再作为普通页面占用导航。
- 快捷入口固定在右上角，菜单采用淡入/淡出出现方式，页面切换也改为轻淡入缩放。
- 新增浅色模式、深色模式、跟随系统模式；浅色模式继续保留原有色板选择。
- 网站收集插件新增“浏览器打开 / 应用内显示”打开方式，并内置两个默认教育网站。

## 影响范围
- Windows / Tauri 前端界面。
- 内置插件 `web-collector`。

## 数据迁移
- 无破坏性迁移。新增设置项会在首次使用时写入本地数据；已有任务、时间块和插件收藏保持不变。
