# Le时间管理 v0.20.0

## 改了什么

- 插件宿主新增局部内容更新动效。插件通过 `innerHTML`、`replaceChildren` 或列表插入更新界面时，新内容会自动柔和显现。
- 插件中心的搜索和筛选结果新增短阶梯入场反馈，筛选变化更容易被感知。
- 所有新增动效都会遵循应用的“减少动态效果”设置和系统的 `prefers-reduced-motion` 偏好。

## 影响范围

- Windows / Android：插件视图与插件中心交互体验优化。
- 微信小程序：无变化。

## 数据迁移

- 无需迁移，现有任务、时间块、设置和插件数据保持不变。
