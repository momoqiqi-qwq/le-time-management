# v0.79.0

## 新增

- 四象限任务卡支持向左或向右滑动删除；越过阈值后，卡片会沿曲线路径飞向象限内的删除目标，并同步缩小、旋转和淡出。
- 未达到删除阈值时，卡片会平滑回到原位；数据只在退场动画结束后移除，并保留现有“撤销删除”入口。
- 支持触摸、鼠标与键盘 `Delete` / `Backspace` 删除，并为“减少动态效果”提供短淡出替代动画。
- 横滑删除与纵向滚动、长按/鼠标拖拽排序进行方向仲裁，避免手势互相抢占。

## 影响范围

- Windows 桌面端与 Android：四象限任务卡交互。
- 无数据迁移；删除与撤销继续复用现有任务数据接口。
