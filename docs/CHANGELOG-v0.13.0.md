# v0.13.0

## 改动内容
- 新增插件通用主题变量别名，旧插件可继承主程序的卡片、边框、次要文字和强调色。
- 完善内置插件深色模式兼容，覆盖卡片、按钮、输入框、标签、提示文字和悬停状态。
- 番茄专注与周度报告改为直接使用主题变量，修复深色模式下白底、淡字和表单对比度不足。
- 插件视图容器增加插件标识，便于按插件做稳定的主题适配和后续维护。

## 影响范围
- Windows / Tauri 前端界面。
- 全部内置插件视图；外部插件也可使用新增的兼容主题变量。

## 数据迁移
- 无数据迁移。主题设置、任务、时间块及插件数据保持不变。
