# v0.18.1

## 改动

- 优化桌面端状态层的时间块日期查询：`blocksOf(date)` 现在按日期缓存已排序结果，减少时间块视图、插件预览、自动排程和冲突检查中的重复全量过滤与排序。
- 为缓存行为增加测试覆盖，确保返回数组不会被调用方污染，并验证时间块更新、删除、整体导入/恢复后的缓存失效。

## 影响范围

- 桌面 / Android 共用前端状态层。
- 不涉及数据结构迁移，已有本地数据与备份格式保持兼容。

## 验证

- `npm test`
- `node ../tools/sync-version.js --check`
